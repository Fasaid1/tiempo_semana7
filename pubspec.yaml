name: weather_app
description: A simple weather app
publish_to: 'none'

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.3
  http: ^0.13.4
  flutter_test:
    sdk: flutter
    

dev_dependencies:
  flutter_lints: ^2.0.0


flutter:
  uses-material-design: true
